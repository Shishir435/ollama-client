// Marker file for the shadcn CLI's framework detector — it only accepts roots
// containing one of next/vite/astro/remix/nuxt/svelte/gatsby/angular configs.
// The real build config lives in wxt.config.ts; WXT generates its own Vite
// config at build time and does not read this file.
import { defineConfig } from "vite"

export default defineConfig({})
