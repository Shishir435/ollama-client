---
import "@/styles/globals.css"

const { title, description, frontmatter } = Astro.props
const _resolvedTitle = title ?? frontmatter?.title
const _resolvedDescription = description ?? frontmatter?.description
---

<BaseLayout title={resolvedTitle} description={resolvedDescription}>
  <div class="min-h-screen bg-background">
    <TopNav />
    <div class="flex">
      <SideNav />
      <main class="flex-1">
        {Astro.slots.has("hero") && (
          <section class="border-b border-border py-10">
            <div class="container-docs">
              <slot name="hero" />
            </div>
          </section>
        )}
        <section class="container-docs py-10">
          <div class="prose-docs">
            <slot />
          </div>
        </section>
      </main>
    </div>
    <Footer />
  </div>
</BaseLayout>
