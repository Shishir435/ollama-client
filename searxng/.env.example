# Local-only defaults for Ollama Client web_search.
# Copy to .env and change values if needed.

SEARXNG_VERSION=latest
SEARXNG_HOST=127.0.0.1
SEARXNG_PORT=8080
SEARXNG_BASE_URL=http://localhost:8080/

